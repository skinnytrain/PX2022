Dashboard application monitoring the stats of LoRaWan sensors in a printing/manufacturin lab.
The sensors send information via a gateway which is then webhooked through to flask server and saved in MongoDB.
User can then log in the portal and monitor the current stats of the lab.
